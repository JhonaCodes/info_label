name: info_label
description: "Data for display in labels or widgets."
version: 1.0.1
homepage: https://github.com/JhonaCodes/info_label

environment:
  sdk: '>=3.3.2 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  alchemist: ^0.7.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:

